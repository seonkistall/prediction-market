apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  labels:
    app.kubernetes.io/name: prediction-market
    app.kubernetes.io/component: api
data:
  NODE_ENV: "production"
  PORT: "3000"
  DB_TYPE: "postgres"
  DB_PORT: "5432"
  JWT_EXPIRES_IN: "7d"
